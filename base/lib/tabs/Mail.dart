import 'package:flutter/material.dart';

class MailTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Mail goes here"),
    );
  }
}