import 'package:flutter/widgets.dart';

class Router{
   static const String login = '/';
   
   Route<dynamic> generateRouter(RouteSettings r){
       switch (r) {
         case :
           
           break;
         default:
       }
   }
}